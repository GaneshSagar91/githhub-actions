name: Manually executed workflow
on: workflow_dispatch
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Building the project
              run: "Project built successfully!"